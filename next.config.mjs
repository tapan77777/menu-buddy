/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["example.com", "res.cloudinary.com"], // 👈 add your actual image domains here
  },
};

export default nextConfig;
